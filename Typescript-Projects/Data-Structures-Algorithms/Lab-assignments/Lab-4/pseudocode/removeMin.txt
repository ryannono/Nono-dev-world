Algorithm removeMin (itemToInsert)
  this: heap class
  output: min item

  if (this.isEmpty())
    return null
  else
    min <- this.data[0]
    max <- this.data[this.size - 1]
    swap min with max
    this.size--
    this.siftdown(this.data[0])

return min;
